const { mdLinks } = require('../index.js');

describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });
  it('Deberia devolver una promesa', () => {
    expect(mdLinks()).toBe(typeof Promise);
  });
})
